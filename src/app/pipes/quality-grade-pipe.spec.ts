import { QualityGradePipe } from './quality-grade-pipe';

describe('QualityGradePipe', () => {
  it('create an instance', () => {
    const pipe = new QualityGradePipe();
    expect(pipe).toBeTruthy();
  });
});
